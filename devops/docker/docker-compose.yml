# Production orchestration.
# Each service's Docker config lives with its project; this file wires them together.
#   Run from repo root:
#     docker compose -f devops/docker/docker-compose.yml up -d --build
include:
  - ./infra.prod.yml
  - ../../api-server/compose.prod.yml
  - ../../webui/compose.prod.yml
