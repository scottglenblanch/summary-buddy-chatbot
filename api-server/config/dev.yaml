# Development configuration
# Non-secret settings live here. Secrets are referenced as ${VAR}
# and resolved from api-server/.env at load time.

FLASK_ENV: development
FLASK_DEBUG: "1"
SECRET_KEY: ${SECRET_KEY}

# Database
DATABASE_URL: postgresql://postgres:${DB_PASSWORD}@localhost:5432/summary_buddy_chatbot

# CORS
CORS_ORIGINS: http://localhost:3000

# OpenAI API
OPENAI_API_KEY: ${OPENAI_API_KEY}

# AWS Configuration
AWS_REGION: us-east-1
AWS_S3_BUCKET: your-s3-bucket-name
AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}

# Set for local MinIO; leave unset for real AWS S3
AWS_S3_ENDPOINT_URL: http://localhost:9000

# PostgreSQL + pgvector Vector Database
PGVECTOR_URL: postgresql://postgres:${DB_PASSWORD}@localhost:5432/summary_buddy_chatbot
PGVECTOR_COLLECTION_NAME: summary_buddy

# RAG Settings
CHUNK_SIZE: "1000"
CHUNK_OVERLAP: "200"
RAG_K_RESULTS: "4"
